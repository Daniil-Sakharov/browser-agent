# =====================================================
# AI BROWSER AGENT - DOCKER ENVIRONMENT
# =====================================================
# Конфигурация специально для Docker окружения
# Копируй этот файл в .env перед запуском в Docker
# =====================================================

# =====================================================
# ANTHROPIC API CONFIGURATION
# =====================================================
# Стандартный API (раскомментируй нужный):
ANTHROPIC_BASE_URL=https://api.anthropic.com/v1
# Или кастомный прокси:
# ANTHROPIC_BASE_URL=https://api.artemox.com/v1

# API ключ (ОБЯЗАТЕЛЬНО замени или передай через переменную окружения!)
# При запуске: ANTHROPIC_API_KEY=sk-ant-... docker-compose up
ANTHROPIC_API_KEY=${ANTHROPIC_API_KEY:-your-api-key-here}
ANTHROPIC_MODEL=claude-sonnet-4-5-20250929
ANTHROPIC_MAX_TOKENS=4096
ANTHROPIC_TEMPERATURE=0.0

# =====================================================
# BROWSER CONFIGURATION
# =====================================================
# ВАЖНО: В Docker ВСЕГДА используй headless=true
BROWSER_HEADLESS=true
BROWSER_USER_DATA_DIR=/app/.browser-data
BROWSER_TIMEOUT=30

# =====================================================
# AGENT CONFIGURATION
# =====================================================
AGENT_MAX_STEPS=30
# ВАЖНО: В Docker INTERACTIVE должен быть false (для background задач)
# Если хочешь интерактивный режим - используй docker attach
AGENT_INTERACTIVE=false
AGENT_SCREENSHOTS=true
AGENT_SCREENSHOTS_DIR=/app/screenshots

# =====================================================
# SECURITY CONFIGURATION
# =====================================================
SECURITY_ENABLED=true
# ВАЖНО: В Docker AUTO_CONFIRM рекомендуется true (нет интерактивного ввода)
SECURITY_AUTO_CONFIRM=true

# =====================================================
# LOGGER CONFIGURATION
# =====================================================
LOG_LEVEL=info
# ВАЖНО: В Docker рекомендуется JSON логи для парсинга
LOG_AS_JSON=true
LOG_FILE=/app/logs/agent.log

# =====================================================
# NOTES:
# =====================================================
# 1. BROWSER_HEADLESS=true - обязательно в Docker
# 2. AGENT_INTERACTIVE=false - для background задач
# 3. SECURITY_AUTO_CONFIRM=true - избежать блокировок
# 4. LOG_AS_JSON=true - для ELK/Splunk интеграции
# 5. Все пути должны быть абсолютными внутри контейнера (/app/...)
# =====================================================
